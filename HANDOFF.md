# HANDOFF

## 项目位置

- 本地目录：`/Users/4paradigm/Documents/New project 3`
- GitHub 仓库：`https://github.com/dcducan/phantymovie-canvas-demo`
- 线上预览：`https://phantymovie-canvas-demo.vercel.app`
- 当前主分支：`main`

## 本地预览

```bash
cd "/Users/4paradigm/Documents/New project 3"
python3 -m http.server 8000
```

访问：

- `http://localhost:8000`

## 本次对话完成内容

### 1. 首页平台作品展示

- 在创作中心首页下方新增 `平台作品展示`。
- 以视频卡片形式展示平台已发布且未下架的视频。
- 支持排序：
  - `最新`
  - `最热`
- 点击视频卡片可以打开放大预览弹窗。
- 首页只展示 `已发布` 且未下架的视频。

### 2. 头像菜单入口

头像弹出卡片中新增：

- `创作主页`
- `审核中心`

顶部中央导航栏已精简为：

- `创作中心`
- `项目管理`
- `公共资产库`
- `素材广场`

### 3. 创作主页

- 新增独立页面：`#/creator-center`
- 顶部展示用户头像和用户名。
- tab 列表当前只有 `视频`，后续可扩展。
- 视频以卡片形式展示。
- 视频卡片字段：
  - 视频封面
  - 视频名称
  - 点赞数量
  - 发布时间
  - 状态标签
- 状态标签已放在封面右下角。
- 点击视频卡片可预览视频。
- 点赞数在创作主页只读展示，不能点赞。
- 视频卡片更多操作：
  - `编辑`
  - `删除`
- 编辑复用发布视频弹窗字段：
  - 视频名称
  - 上传封面
  - 上传视频内容
- 删除有二次确认弹窗。

### 4. 发布视频

发布视频需要填写：

- 视频名称
- 视频封面
- 视频内容

视频名称允许重复。

发布后：

- 视频状态为 `待审核`
- 发布时间为空
- 弹窗提示：

```text
视频提交成功，已进入审核阶段，审核通过后将发布在创作中心首页
```

### 5. 审核中心

- 新增独立页面：`#/review-center`
- tab 列表当前只有 `视频`，后续可扩展。
- 展示所有账户上传的视频，包括：
  - 待审核
  - 审核不通过
  - 已发布
  - 已下架
- 视频列表字段：
  - 视频名称
  - 视频封面
  - 点赞数量
  - 发布时间
  - 状态
  - 操作
- 点击视频封面可预览视频。
- 发布时间以审核通过时间为准；没有发布时间时展示 `--`。
- 状态筛选是右侧下拉框：
  - `全部`
  - `待审核`
  - `审核不通过`
  - `已发布`
  - `已下架`
- 默认筛选为 `全部`。

审核中心操作列固定展示两个按钮：

- `下架 / 上架`
- `审核`

不可用的操作会置灰并禁用。

状态规则：

- `待审核`：可执行 `审核`。
- `审核不通过`：两个操作都置灰。
- `已发布`：可执行 `下架`。
- `已下架`：可执行 `上架`。

审核操作：

- 点击 `审核` 打开审核弹窗。
- 可选择：
  - `审核通过`
  - `审核不通过`
- 审核通过时写入当前时间作为发布时间。

上下架操作：

- 点击 `下架` 有二次确认。
- 下架后状态展示为 `已下架`，操作变为 `上架`。
- 点击 `上架` 有二次确认。
- 上架后状态恢复为 `已发布`。

## 关键文件

- `index.html`
  - 页面结构
  - 头像菜单入口
  - 创作主页
  - 审核中心
  - 发布、预览、审核等弹窗
- `script.js`
  - 视频数据
  - 路由状态
  - 发布视频
  - 编辑/删除
  - 审核/上下架
  - 状态筛选
  - 首页发布视频过滤
- `styles.css`
  - 视频卡片
  - 审核列表
  - 状态标签
  - 筛选下拉框
  - 弹窗样式

## 当前数据说明

视频数据仍是纯前端本地模拟数据，保存在 `localStorage`：

- key：`phanty-movie-videos-state`

默认数据中包含多个状态：

- 已发布
- 已下架
- 待审核
- 审核不通过

为了兼容旧浏览器缓存，`script.js` 中会自动补齐新增示例数据。

## 当前页面 / 视图梳理

本项目是单页静态应用，入口为 `index.html`，通过 `script.js` 中的 `state.currentView` 与 URL hash 切换视图。主要路由和页面如下。

### 1. 创作中心首页

- 路由：`#/home`，空 hash 或 `#/` 也会进入该页。
- 页面名称：`创作中心`
- 入口：顶部导航 `创作中心`
- 主要功能：
  - 展示三种创作入口：`工作流`、`全自动 AI 生成`、`无限画布`。
  - `工作流` 和 `无限画布` 的开始按钮会打开创建项目弹窗；`全自动 AI 生成` 当前禁用，展示为即将开放。
  - 展示 `平台作品展示` 视频列表。
  - 视频列表支持 `最新` / `最热` 排序。
  - 只展示状态为 `已发布` 且未下架的视频。
  - 点击视频卡片打开视频预览弹窗。
  - 首页底部提示项目会自动保存至个人库。

### 2. 项目管理 / 我的项目

- 路由：`#/projects`
- 页面名称：`我的项目`
- 入口：顶部导航 `项目管理`
- 主要功能：
  - 展示项目卡片列表。
  - 支持搜索项目名称。
  - 支持按时间正序、时间倒序、名称正序、名称倒序排序。
  - 支持创建新项目。
  - 支持批量操作、全选、批量删除。
  - 支持单个项目上传封面、编辑项目名称、删除项目。
  - 点击项目卡片打开 `创作模式` 弹窗。
  - 创建项目后也会打开 `创作模式` 弹窗。

### 3. 工作流首页

- 路由：`#/project/:projectId/workflow`
- 页面名称：`工作流首页`
- 入口：项目卡片打开 `创作模式` 弹窗后，选择 `工作流` 进入。
- 主要功能：
  - 作为项目进入工作流模式后的首页，默认展示 `快捷创作`。
  - 左侧展示当前项目名称、画面比例、风格，以及当前模式 `工作流`。
  - 左侧工作流菜单包含：快捷创作、剧本分析、项目资产库、拍摄台、模型对话广场、生成历史。
  - 快捷创作展示四步流程、总体进度、生成确认弹窗与跨页回流引导。
  - 剧本分析已具备整理剧本、确认资产清单、生成分镜脚本的简化交互；项目资产库当前仍为快捷创作跳转的空白承接页。
  - 拍摄台保留既有关键帧、视频制作、配音配乐及时间线能力。
  - 左侧 `切换模式` 会打开创作模式弹窗，可切换到无限画布。

### 4. 项目画布列表 / 无限画布首页

- 路由：`#/project/:projectId/canvases`
- 页面名称：`{项目名} · 画布列表`
- 入口：项目卡片打开 `创作模式` 弹窗后，选择 `无限画布` 进入；也可从画布详情返回。
- 主要功能：
  - 展示当前项目下的画布列表。
  - 作为项目进入无限画布模式后的首页。
  - 左侧展示当前项目名称、画面比例、风格，以及当前模式 `无限画布`。
  - 支持搜索画布名称。
  - 支持排序。
  - 支持新建画布。
  - 支持批量选择和批量删除画布。
  - 支持单个画布上传封面、重命名、删除。
  - 点击画布卡片进入画布详情页。
  - 左侧 `切换模式` 会打开创作模式弹窗。

### 5. 无限画布详情

- 路由：`#/project/:projectId/canvas/:canvasId`
- 页面名称：当前画布名称
- 入口：项目画布列表中点击画布卡片。
- 主要功能：
  - 展示可拖拽、缩放、平移的画布编辑器。
  - 支持空画布状态。
  - 支持添加节点面板，添加文本、图片、视频、音频、上传资源等节点入口。
  - 支持双击画布创建节点。
  - 支持图片节点、文本节点、720 全景节点、全景截图组节点。
  - 支持节点选择、拖拽、删除、复制、粘贴、创建副本。
  - 支持节点左右添加相邻节点，并自动连线。
  - 图片节点工具栏包含反推、全景、多角度、打光、九宫格、擦除、洗图、宫格切分等入口，其中反推和洗图有扣星钻确认弹窗。
  - 支持图片节点上传替换图片。
  - 支持摄像机控制弹层，可设置相机、镜头、焦距、光圈，并开关摄像机控制。
  - 支持由图片节点生成 720 全景节点。
  - 全景节点支持当前视角截图、4 大视角截图、12 大视角截图、重置视角、构线参考图、全屏预览。
  - 左侧画布操作栏新增模板库入口，图标使用 `layout-board` 形态。
  - 模板库支持官方模板 / 自定义模板切换，支持按模板名称或母版标题检索。
  - 模板库以三列小卡片展示，卡片下方仅展示模板名称；hover 卡片后显示 `添加至画布` 操作，自定义模板额外显示 `删除`。
  - 官方模板内置角色定妆画布、场景探索画布；点击 `添加至画布` 会将模板节点复制到当前画布视口中心附近。
  - 自定义模板保存在当前项目的 `canvasTemplates` 字段，随项目状态写入 localStorage。
  - 支持右键节点打开上下文菜单：保存到资产库、添加为模板、创建副本、复制、粘贴、删除。
  - 右键 `添加为模板` 会打开弹窗，可输入模板标题并上传模板封面；确认后保存为当前项目的自定义模板。
  - 自定义模板删除有二次确认弹窗，官方模板不可删除。
  - 画布名称右侧支持分享画布，弹窗展示分享链接、一键复制入口、访问权限开关（公开访问 / 仅自己可见）。
  - 点击分享弹窗中的 `一键复制` 会进入分享查看页。
  - 支持底部缩放控制与百分比重置。

### 6. 画布分享查看页

- 路由：`#/share/project/:projectId/canvas/:canvasId`
- 页面名称：当前画布名称
- 入口：无限画布详情页打开分享弹窗后，点击 `一键复制` 进入。
- 主要功能：
  - 用于展示分享链接打开后的画布查看状态。
  - 保留顶部右侧操作区。
  - 左上角显示 PhanthyMovie logo。
  - 左侧画布操作栏隐藏，节点编辑入口停用。
  - 左上角 logo 旁提供 `复制画布` 操作。
  - 点击 `复制画布` 打开弹窗，可选择目标项目并设置画布名称。
  - 确认复制后会在目标项目下创建当前画布副本，并进入复制后的普通画布详情页。

### 7. 创作主页

- 路由：`#/creator-center`
- 页面名称：`创作主页`
- 入口：头像菜单中的 `创作主页`
- 主要功能：
  - 展示创作者头像和用户名。
  - 当前只有 `视频` tab。
  - 展示账号上传的视频卡片。
  - 视频卡片展示封面、视频名称、视频文件名、点赞数量、发布时间、状态标签。
  - 点击视频封面区域可打开视频预览弹窗。
  - 支持发布视频。
  - 支持视频更多操作：编辑、删除。
  - 编辑视频复用发布视频弹窗，可修改视频名称、封面、视频内容。
  - 删除视频会走确认删除弹窗。

### 8. 审核中心

- 路由：`#/review-center`
- 页面名称：`审核中心`
- 入口：头像菜单中的 `审核中心`
- 主要功能：
  - 展示审核中心头像和用户名。
  - 当前只有 `视频` tab。
  - 展示所有上传视频，包括待审核、审核不通过、已发布、已下架。
  - 右侧状态筛选支持：全部、待审核、审核不通过、已发布、已下架。
  - 视频列表展示视频名称、封面、点赞数量、发布时间、状态、操作。
  - 点击封面可打开视频预览弹窗。
  - `待审核` 视频可执行审核。
  - 审核弹窗支持 `审核通过` 和 `审核不通过`。
  - 审核通过会写入当前时间作为发布时间，并变为已发布。
  - 已发布视频可执行下架；已下架视频可执行上架。
  - 审核不通过视频的审核和上下架操作均不可用。

### 9. 顶部与账号浮层

- 顶部导航：
  - `创作中心`：进入首页。
  - `项目管理`：进入我的项目。
  - `公共资产库`：当前只有导航按钮，尚未绑定独立视图。
  - `素材广场`：当前只有导航按钮，尚未绑定独立视图。
- 顶部操作：
  - `限时活动`：打开限时活动弹窗，包含活动说明、倒计时和去充值按钮。
  - `充值中心`：当前为按钮展示，未绑定独立页面。
  - 星钻余额：展示余额。
  - 通知按钮：打开通知中心浮层。
  - 头像按钮：打开账号菜单浮层。
- 头像菜单：
  - 展示安全提醒、用户信息、专业版信息、团队星钻余额。
  - `创作主页` 和 `审核中心` 已绑定真实视图。
  - `联系客服` 会打开客服二维码弹窗。
  - 账号设置、交易记录、我的消耗明细、AI 水印设置、退出登录当前仅展示入口。

### 10. 全局弹窗 / 模态组件

- 创建 / 编辑项目弹窗。
- 新建 / 重命名画布弹窗。
- 删除确认弹窗，同时复用于项目、画布、视频删除，以及视频上下架确认。
- 创作模式弹窗：包含工作流、全自动 AI 生成、无限画布；工作流进入工作流首页，无限画布进入项目画布列表，全自动 AI 生成禁用。
- 限时活动弹窗。
- 联系客服弹窗。
- 视频预览弹窗。
- 发布 / 编辑视频弹窗。
- 发布成功弹窗。
- 审核视频弹窗。
- 分享画布弹窗。
- 复制画布弹窗。
- 画布内反推确认弹窗。
- 画布内洗图确认弹窗。
- 全景图全屏预览浮层。
- 画布添加自定义模板弹窗。
- 自定义模板删除确认弹窗。

### 11. 本轮新增：工作流拍摄台 / 关键帧生成

- 路由：`#/project/<projectId>/workflow`
- 拍摄台页面已从原来的步骤卡片改为单页工作台布局，核心围绕关键帧生成、视频生成、配音补齐三类后续任务展开。
- 左侧项目导航已调整为：
  - `快捷创作`
  - `剧本分析`
  - `项目资产库`
  - `拍摄台`
  - `模型对话广场`
- 左侧导航支持收起，收起按钮位于导航栏右下角。
- 顶部任务 tab 当前展示：
  - `关键帧生成`
  - `关键帧视频生成`
  - `生成视频片段`
  - `配音补齐`
- `集 · 场` 区域改为两个美化后的下拉框，分别选择集和场。
- 下拉选择会同步切换：
  - 当前剧本
  - 分镜表
  - 关键帧结果
- `分镜表` 标题右侧会展示当前集、当前场、共几镜。
- 分镜表每条镜头以折叠列表展示：
  - 默认展示镜号和秒数。
  - 点击镜头后展开完整分镜剧本。
  - 展开内容会展示该镜关联的角色、场景、道具、定妆照。
  - 素材按类型分组展示，支持多个角色 / 多个场景 / 多个道具 / 多张定妆照。
- `生成当前镜` 和 `整场多宫格` 按钮放在分镜表右下角。
- 点击 `生成当前镜` / `整场多宫格` 会打开 `关键帧生成` 弹窗。
- `关键帧生成` 弹窗包含：
  - 可编辑提示词文本框。
  - 生成张数。
  - 图像模型。
  - 清晰度下拉框。
  - 画面比例。
  - 参考图列表。
  - 参考图支持删除。
  - 支持上传参考图入口。
  - 底部右侧展示星钻图标和数量，图标与右上角导航栏星钻一致。
- 关键帧结果卡片只保留：
  - `第几集 · 第几场 · 第几镜`
  - 或 `第几集 · 第几场 · 整场多宫格`
  - 操作按钮：`查看详情`、`编辑`
- 点击关键帧卡片本身会打开图片预览。
- 点击 `查看详情` 会打开 `关键帧详情`，只展示：
  - 生成参数
  - 参考图
  - 提示词
  - 不再展示原始大图。
- 点击 `编辑` 会打开图片编辑工作台，参考截图做了深色全屏编辑布局：
  - 左侧为大图预览。
  - 顶部有返回、序号、分页 / 当前操作信息。
  - 图片上方有缩略图切换。
  - 左右有上一张 / 下一张按钮。
  - 底部有修改要求输入框、模型选择和生成按钮。
  - 右侧有提示词面板。
  - 右侧工具菜单包括元素位移、剪裁图像、一键剪裁图像、画质提升、扩图、消除、局部重绘。
- 关键帧结果区域底部新增一排小工具：
  - `镜头控制`
  - `机位控制`
  - `多角度分镜`
  - `分镜推演`
- 页面底部新增时间线区域：
  - 预览播放按钮。
  - 导出按钮。
  - 关键帧线。
  - 视频线。
  - 音频线。
  - 关键帧卡片可拖入时间线，当前为前端动效模拟。

### 12. 本轮新增：首页 Banner 与画布音频节点

- 首页 `开启您的创作旅程` 上方新增宣传 banner 位。
- Banner 采用多块矩形横向展开样式：
  - 默认展开第一块。
  - 点击其他卡片会丝滑展开。
  - 收起卡片文字保持正向显示。
  - Banner 宽度与下方组件对齐，并做了响应式适配。
  - Banner 与 `开启您的创作旅程`、`平台作品展示` 之间的间距已加大。
  - 选中 banner 去掉外边框。
- 画布添加节点中新增音频节点能力：
  - 点击音频节点可在画布中添加音频节点。
  - 音频节点包含基础上传区域、文本输入区域。
  - 除基础设置外，新增 `音色设置` 和其他设置入口。
  - `音色设置` 使用 icon 展示，点击会弹出音色设置弹窗。

### 13. 本轮新增：关键帧编辑、时间线预览与活动隐藏

- 关键帧结果编辑弹窗重构为大弹窗：
  - 左侧展示当前关键帧大图和缩略图列表。
  - 右侧展示提示词、参考图、模型及模型参数。
  - 支持 `改图`、`拆图`、`剪裁`、`扩图`、`抠图`、`擦除`、`重绘`、`洗图`、`标注` 操作入口。
  - 抠图、洗图会弹出星钻消耗确认，确认后在缩略图中新增生成中结果。
  - 大图右上角增加 `保存到` 下拉，可选择保存到项目资产库、团队资产库或素材广场。
  - 大图右上角保留下载；非原图结果可添加到关键帧结果或删除。
- 关键帧生成弹窗的 `上传参考` 改为打开独立上传参考弹窗：
  - 支持本地、项目资产库、团队资产库、素材广场四种来源。
  - 资产库和素材广场支持角色、场景、服装、道具、音频、其他分类。
  - 资产卡片只展示预览图和名称。
- 关键帧结果区域调整：
  - 结果卡片恢复 `删除` 和 `编辑` 操作。
  - 操作按钮与 `第几集 · 第几场 · 第几镜` 信息同一行，说明靠左，按钮靠右。
  - 增加筛选、上传、拼图、清空操作入口。
- 时间线模块调整：
  - 支持展开/收起。
  - 关键帧线、视频线、音频线可隐藏/展示。
  - 关键帧、视频、音频片段可选中、拖动、调整时长，并支持在 Playhead 裁剪。
  - 右键片段会打开菜单，菜单中可删除。
  - 时间线顶部左侧包含 `预览`、`导出`、`裁剪`，右侧包含三条线显示开关和 `收起`。
- 时间线预览弹窗调整：
  - 上方展示关键帧预览和视频/音频预览。
  - 下方保留可编辑时间线，可在预览状态下继续编辑时间线。
  - 时间线操作栏上方增加统一播放器，包含播放/暂停图标、进度、时间和音量控制。
  - 删除预览弹窗中的辅助说明文案。
- 工作台布局补充：
  - `集 · 场` 和 `分镜表` 右侧增加拖拽把手，可左右调整宽度。
  - `生成当前镜`、`整场多宫格`、`预览` 按钮圆角统一。
- 限时活动结束后已隐藏相关内容，代码未删除：
  - 顶部 `限时活动` 入口隐藏。
  - 首页活动 banner 隐藏。
  - 限时活动弹窗隐藏。

### 14. 本轮新增：拍摄台视频制作

- 拍摄台顶部任务 tab 支持在 `关键帧生成`、`视频制作`、`配音配乐` 间切换；本轮已完整实现视频制作工作区。
- 视频制作保留集·场剧本和分镜表的信息展示，不再提供关键帧生成操作。
- 右侧结果区提供：
  - 视频生成器
  - 该场视频
- 视频生成器参数顺序：
  1. 生成模式（当前为 `全能参考`）
  2. 选择模型
  3. 生成镜头（可选择具体镜头或不指定镜头）
  4. 提示词、参考资源和其他模型参数
- 支持设置画面比例、分辨率、时长、是否配音；生成模式、模型和参数会保存到生成结果，且可通过“复制参数到生成器”恢复。
- 视频提示词改为可编辑输入区：
  - 输入 `@` 可选择已添加的参考图、参考视频、参考音频。
  - 下拉候选项展示资源缩略图。
  - 插入后的 `@资源` 以带缩略图的标签形式与普通文本区分。
- 参考图样式与关键帧生成保持一致，支持：
  - 上传参考图。
  - 从当前场关键帧浮窗中选择，浮窗可按镜头筛选。
  - 删除自动带入或手动添加的参考图。
- 参考视频与参考音频支持上传、预览播放、音量调节和删除。
- 点击“生成视频”后会立即在“该场视频”新增生成中结果，模拟完成后切换为已完成状态。
- 已生成视频：
  - 命名格式为 `第m集·第N场·第s镜` 或 `第m集·第N场·不指定镜头`。
  - 默认仅展示缩略图、名称、时长和状态；点击卡片展开查看生成模式、模型、参数、提示词及参考资源。
  - 视频、参考视频、参考音频均可打开预览弹窗，弹窗提供播放/暂停及音量控制。
  - 支持复制参数到生成器、加入时间线、下载和删除；删除前有“是否确认删除，删除内容可在回收站找回”二次确认。
- 时间线补充：
  - 视频线和音频线的片段名称改为显示在各自片段下方。
  - 关键帧线、视频线、音频线最右侧均增加仅图标的上传入口。
  - 上传入口复用视频生成器的上传资源弹窗，并根据轨道限定关键帧、视频或音频类型；确认后会向对应轨道添加片段。
- 修复工作台内输入框和下拉框点击后失焦的问题：现在单击即可编辑或展开选择。

### 15. 本轮新增：工作流生成历史

- 工作流左侧导航栏底部新增 `生成历史` 入口，图标使用历史 / 时钟形态。
- 生成历史仅在工作流内展示，不改变其他页面。
- 页面展示当前项目内图片、视频、音频的生成或编辑历史。
- 类型 tab 包含：
  - `图片`
  - `视频`
  - `音频`
- 每个 tab 后展示对应类型数量，默认进入图片 tab。
- 保留来源筛选，来源固定为：
  - `全部来源`
  - `模型对话广场`
  - `拍摄台`
  - `项目资产库`
- 保留排序，当前仅支持：
  - `时间正序`
  - `时间倒序`
- 排序和筛选样式为胶囊按钮 + 深色下拉菜单，当前选项展示勾选。
- 资产以卡片形式展示：
  - 图片展示缩略图。
  - 视频展示封面和时长。
  - 音频展示音频占位视觉和时长。
  - 来源标签显示在缩略图左上角。
  - 卡片底部展示 `创建于` 时间和下载 icon。
  - 卡片不展示资产名称、大小、删除按钮。
  - 卡片 hover 不显示青绿色边框。
- 创建时间格式统一为 `YYYY年MM月DD 日 HH:mm:ss`。
- 图片、视频、音频均支持预览：
  - 预览弹窗标题统一为 `预览`。
  - 预览弹窗不展示来源标签。
  - 图片预览仅展示大图。
  - 视频预览提供播放 / 暂停、进度条和音量调节。
  - 音频预览提供音频占位视觉、播放 / 暂停、进度条和音量调节，不展示音频名称。
  - 预览弹窗底部展示 `创建于` 时间和下载 icon。
- 生成历史不支持删除、永久删除、恢复、批量操作和搜索。
- 当前生成历史数据仍为纯前端本地模拟数据，保存在 `localStorage`：
  - key：`phanty-movie-workflow-recycle-v1`
  - 为兼容早期回收站演示数据，读取时会把旧字段映射为生成历史字段。
  - 加载时会自动补齐内置示例数据，包括音频示例。
- 拍摄台视频删除弹窗文案已同步调整为：`是否确认删除？生成记录仍可在生成历史查看`。

### 16. 本轮新增：生成历史弹窗与操作优化

- 工作流生成历史和画布列表页生成历史的预览页面：
  - 缩略图 hover 时取消中央预览按钮。
  - 仅保留右下角下载和预览操作。
  - 下载与预览 icon 统一尺寸、颜色、透明度和 hover 样式。
  - 预览页关闭按钮位于右上角，不带边框或外框背景。
  - 关闭按钮与下载按钮之间预留独立空间，避免重叠。
- 画布操作页生成历史：
  - 历史内容改为居中小弹窗，增加右上角关闭按钮。
  - 关闭按钮不带边框或外框背景，保留关闭功能。
  - 关闭后不会因画布详情重新渲染而自动重新出现。
  - 缩略图 hover 时取消中央预览按钮，右下角保留预览、下载、使用三个操作。
  - 多选工具栏除下载外增加批量使用操作，可将选中资源依次添加到画布。
  - 预览页保留左上角返回操作，不增加额外关闭按钮；预览弹窗尺寸与历史小弹窗一致。
- 上述生成历史改动保持工作流历史、画布列表历史和画布操作页各自的页面行为边界，不改变历史数据存储方式。

### 17. 本轮新增：快捷创作原型引导

- 工作流默认进入 `快捷创作`，不改动平台顶部导航或既有拍摄台工作台内容。
- 快捷创作为单页四步流程：`剧本分析`、`准备项目资产`、`生成整场多宫格关键帧`、`进入视频制作`。
- 顶部进度控制条固定在快捷创作工作区顶部；左侧圆形总体进度按关键节点累计：整理剧本 10%、资产清单 20%、分镜脚本 25%、项目资产 50%、多宫格关键帧 75%、视频制作 100%。右侧四步会随状态持续点亮系统强调色。
- 四张流程卡统一尺寸，采用左侧纵向进度线；未开始为空、执行中逐步填充、完成后保留满格。任务执行时查看结果入口保留，生成操作置灰。
- 剧本分析包含三个内部子步骤：`1 整理剧本`、`2 资产清单`、`3 生成分镜脚本`。子步骤摘要是任务结果快照，始终保持中性灰，不因项目资产库或分镜表后续编辑而变化；执行文案统一为 `正在执行···`。
- 上传剧本弹窗支持 `上传文件` 与 `粘贴文本`，展示 TXT / DOCX / FDX 格式提示；不展示模型、参数或积分，点击 `确认` 后开始整理。
- 提取资产清单与生成分镜脚本均先展示积分确认弹窗：
  - 提取资产：72 星钻，确认后执行。
  - 生成分镜脚本：54 星钻，提供紧凑、标准（推荐）、舒缓三种镜头节奏及预计镜头数。
- 剧本分析不允许重新执行。分镜脚本完成后自动完成剧本分析并解锁项目资产，不再需要点击“继续下一步”。
- `准备项目资产`展示的是项目资产库当前数量，而不是剧本分析的提取快照：初始为角色 0、服装 0、场景 0、道具 0；用户确认资产并开始生成分镜脚本时，演示数据同步为角色 3、服装 3、场景 4、道具 6。后续项目资产库的手动增删应更新同一数据源。项目资产图片生成只更新“已生成 70 张项目资产”，不改变四类资产数量。
- `生成整场多宫格关键帧`展示当前分镜表结构，而不是分镜任务快照：分镜未完成时显示 `解析结果：—`、`提示词 — / — 未就绪`；分镜完成后展示当前的集、场、镜数量，演示数据为 `1 集 5 场 25 镜`。分镜表增删场次或镜头后，应同步更新关键帧统计和提示词总数。
- 分镜提示词是关键帧生成前置校验，不是第三步的解锁条件：项目资产完成后第三步即可点击；若提示词未全部就绪，弹出轻量拦截弹窗，显示完成进度并引导前往剧本分析。剧本分析分镜页提供“生成全部提示词 · +150”，完成后提示词进度变为 `25 / 25 已就绪`，关键帧可进入生成确认弹窗。
- 项目资产和关键帧均支持重新生成；重新生成只补充图片或关键帧，不覆盖旧结果。项目资产完成文案统一为“已生成 70 张项目资产”。
- 视频制作是跳转引导，不发起生成任务。关键帧完成后，第四步保留状态“当前进行中”，并显示“关键帧已就绪可进入拍摄台继续制作”；点击进入拍摄台视频制作后，总进度到 100%。
- `剧本分析` 是简化三阶段工作台；`项目资产库` 当前仍是快捷创作跳转的空白承接页。剧本分析、项目资产库与拍摄台关键帧阶段均可通过查看结果入口跳转。
- 快捷创作浮窗为跨页面进度入口：用户启动第一步后，在所有非快捷创作页面展示当前快捷创作进度；进入拍摄台视频制作阶段后自动消失。浮窗可收起，快捷创作导航和收起态入口当前使用 Mouse 图标。
- 浮窗提示语为“确认结果后，可返回继续下一步”，并提供返回快捷创作按钮。

### 18. 本轮新增：模型对话广场

- 工作流左侧新增 `模型对话广场`，以全屏对话工作区承载视频生成、图片生成与文本创作；默认自动创建一条空白对话。
- 对话列表支持：新建、日期分组（置顶 / 今天 / 昨天 / 具体日期）、置顶、列表内重命名、删除二次确认。
- 空白对话提供三张快捷引导卡：`关键帧生成视频`、`生成图片`、`剧本创作`。点击后会回填示例提示词并同步切换推荐类型、模型和参数；仅预填，不自动发送。
- 输入框固定在页面底部，支持视频 / 图片 / 文本三种类型与对应的模型选择：
  - 视频：生成模式、比例、清晰度、生成数量、时长。
  - 图片：比例、清晰度、生成数量、尺寸。
  - 文本：模型选择。
  - 实际可见参数、参考文件类型与数量应以具体模型能力配置为准。
- 模型选择器已补充生文、生图、生视频模型的能力说明文案，方便用户快速判断适用场景。
- 输入框支持上传图片、视频、音频和文档：
  - 图片展示缩略图与名称；视频展示封面、名称和时长；音频展示波形；文档展示文件图标。
  - 本地上传展示原始文件名；资产库素材优先展示资产名称；不再使用“图 1”等自动编号。
  - 素材可通过输入框键入 `@` 或底部 `@` 按钮引用；底部按钮的候选菜单展示在按钮正上方。
- 上传菜单新增系统素材入口，可从项目资产库、团队资产库、素材广场选择素材：
  - 分类 Tab 下支持搜索；项目/团队显示“搜索资产”，素材广场显示“搜索素材”。
  - 素材卡右上角固定复选框，底部显示“已选 N 项”；点击“添加”后批量加入输入框。
  - 悬浮图片素材时右下角出现预览图标，点击后打开纯图片放大预览。
- 对话结果按类型展示：视频生成展示一个或多个视频，图片生成展示一张或多张图片，文本创作展示文字回复。图片和视频结果均支持下载、保存至、添加至参考与预览。
- 保存至支持项目资产库、团队资产库和素材广场的表单式流程；视频保留完整选项展示，但不支持的位置和分类置灰。
- 本轮新增资源包括模型对话广场插图、模型/上传/操作图标等，均位于 `assets/images/model-chat/`、`assets/images/model-chat-ai-logo.png` 与 `assets/icons/`。

### 19. 本轮更新 配音配乐与音色克隆

- 音频生成器支持 MiniMax Speech 2.8 HD、2.8 Turbo、2.6 HD、2.6 Turbo，以及豆包语音合成大模型 2.0。音乐创作保留入口，本期暂不做；音色克隆内嵌在文生音频中。
- 已选模型决定克隆供应商，点击克隆直接进入添加样本，不再经过重复的模型或音色选择。MiniMax 使用 Voice Clone；豆包使用声音复刻 2.0，提供标准版、表现力增强版。
- 两家音色隔离；模型切换保留正文，分别保存音色与参数。MiniMax 语言为自动检测、中文、英语、日语、法语；豆包跟随音色默认，语言选择禁用。
- MiniMax 支持整体情绪、分段情绪、停顿及长文模式；2.8 额外支持语气词。停顿提供预设与自定义，语气词及停顿以中文高亮标签插入。切换模型后提交会拦截不兼容标签。
- 分段情绪的实际实现：选中文字后打开情绪面板，选择后生成不可直接编辑的整体标签；点击关闭图标移除情绪并恢复原文。修改片段文字或情绪需先移除再编辑或标记，尚不支持点击标签直接改情绪。
- 豆包提供表演要求、上文参考，正文占位提示为“输入需要合成语音的文字”。
- 克隆弹窗固定标题“音色克隆”，步骤为添加样本、音色信息、创建结果。支持浏览器录音、上传、样本校验、1–20 字符名称计数、同供应商同空间重名校验与声音授权。
- 平台演示统一采用临时音色流程：创建后 7 天内首次正式合成，成功后移除临时标签；到期未使用则失效。选择或试听不延长有效期。两家供应商真实机制仍须分别适配，不能将平台演示规则视为官方机制相同。
- 首次调用弹窗标题为“临时音色调用费用确认”，按钮为取消、确认。星钻换算为 1 元人民币 = 100 星钻；当前克隆费用配置为 MiniMax 990 星钻、豆包 10,000 星钻。豆包费用尚未核验，不作为官方报价。
- 删除音色弹窗：标题“确认删除”，正文“删除后音色将无法找回，已经生成的音频不受影响”，取消/确认按钮。正文使用普通灰色，与其他删除提醒一致。
- 音频生成器使用 audio-waveform 图标，本场音频使用 headset 图标；调整一级页签、波形、滑动条及音频名称输入框布局。
- 拍摄台三个阶段均支持独立收起/展开集场与分镜表；配音配乐分镜表不提供关键帧生成按钮。
- 核心新增文件为 `doubao-voice.js`，由 `index.html` 在 `script.js` 之前加载。

### 20. 本轮更新 时间线导出

- 新增 `timeline-export.js`，导出弹窗采用宽版双栏布局，展示文件名称、时长、视频导出与音频导出。
- 支持视频与音频合并、仅视频、仅音频三种配置。关闭某一项时对应参数置灰保留，不隐藏。
- 视频包含清晰度、比例（默认原视频比例）、格式、帧率、码率、编码；音频包含格式、质量、采样率。合并视频使用 AAC，仅音频可选 MP3/WAV，WAV 质量显示 PCM 16-bit。
- 校验文件名及所选轨道是否有片段。当前没有真实素材合成服务，导出会明确提示原型状态，不产生真实成片。

### 21. 本轮文档与后续协作约定

- 已在对话中输出配音配乐和时间线图文 PRD，配音配乐后来按当前代码重写为功能版，并补充分段情绪独立章节与截图。
- 用户明确要求：PRD 描述最终功能、入口、操作、字段、状态、异常及关联规则，不复述“删除旧文案”“不再展示”等历史修改过程。必要文案直接写最终值，样式优先用截图表达。
- 以可到达的当前页面和实际代码交互为依据，不能把讨论过但未实现的交互写成现有能力；演示状态与正式接入要求分开说明。
- 截图保存在 `artifacts/audio-prd/` 与 `artifacts/timeline-prd/`。本次检查时，`artifacts/audio-prd/拍摄台配音配乐PRD.docx` 为旧版；此前交付的 `拍摄台配音配乐PRD功能版.docx` 与时间线 DOCX 当前不在原交付路径，后续使用前需重新定位，不要将旧版误当新版。
- 本次 Git 提交仅包含运行代码和交接文档；PRD、截图、渲染中间文件、包缓存及无关模型对话素材不纳入部署提交。

### 22. 当前真实接入边界

- 录音、文件解码与前端校验可运行；克隆训练、克隆试听、语音合成和扣费尚未接入。音色信息目前保存在浏览器 localStorage：`workflow-minimax-voices-v1`、`workflow-doubao-voices-v1`。
- 官方音色或已使用音色提交时提示合成服务未接入；临时音色确认后仅新增带 `mock: true` 的模拟结果并更新本地状态，没有真实音频或实际扣费。
- 分段情绪的拆段合成拼接、长文异步任务、模型能力映射与账务异常处理尚需接入。
- 本场音频波形与时长为展示数据；复制参数尚未完整恢复高级设置。真实播放、下载、生成历史持久化、草稿场景保存需联调。
- “加入时间线”按钮当前仅触发视觉反馈，尚未创建真实音频片段；不能以此宣称完成媒体引用和导出。

### 23. 本轮新增：产品首页新手引导

- 新增独立 `home-onboarding.js` 与 `home-onboarding.css`；`index.html` 仅添加这两个文件的加载引用，原有 `script.js` 与 `styles.css` 未修改。
- 首次进入首页展示三步引导：点击工作流卡片的开始入口、填写项目设置并创建、在创作模式弹窗选择模式。引导观察现有弹窗状态，不替代创建项目或模式切换逻辑。
- 首页引导接入本地固定版本 Driver.js 1.8.0（MIT，位于 `assets/vendor/driverjs/`）。全部提示和镂空遮罩位于独立悬浮层，不向原页面或弹窗内部插入内容，不修改原布局。
- 引导提示采用首次对话气泡版外观：冰蓝色底、深色文字、青绿色按钮、24px 圆角与11px指向尾巴；保留 Driver.js 悬浮高亮交互，不插入页面布局。
- 标题前使用用户提供的 signpost.svg 图标；进度仅展示 01/06 格式的当前步数与总步数，以 13px 字号放在气泡左下角，导航按钮保留在右侧；「跳过」位于右上角，与标题首行对齐，标题预留空间避免重叠，前进和返回同步更新；无 STEP 文案、底板、进度灯条或额外动效。
- 三个阶段中，项目设置细分为名称、比例、风格、创建按钮逐项高亮。包含高亮平滑切换、自动提示定位与箭头，系统减少动态效果设置下关闭动画。
- 提供跳过、Escape 退出，以及首页右下角悬浮的「新手引导」重看入口。完成或跳过状态单独存储于 `phanty-home-onboarding-v1`，不写入业务数据。
- 模式选择引导文案为「根据你的创作需求，选择合适的模式开始创作。三种模式共享项目资产，后续可随时切换。」不逐项介绍模式。
- 全自动 AI 生成维持禁用和即将开放状态；工作流和画布内部未增加引导。
- 最新浮层版浏览器验证：逐项高亮、输入名称、选择竖屏和动漫风格、创建并进入无限画布、跳过后遮罩移除及交互恢复、重新查看；提示 DOM 不在弹窗内部，未发现浏览器错误日志。
- 上一轮推送被自动审批拒绝，未取得额外上线授权；当前更新仅本地预览与提交，等待用户明确授权再推送部署。

### 24. 本轮新增：工作流与画布引导

- 新增独立 `workspace-onboarding.js`，`index.html` 增加加载引用；复用 `home-onboarding.css`，首页引导样式及业务脚本均未改动。
- 工作流首次进入时启动六步模块介绍：快捷创作、剧本分析、项目资产库、拍摄台、模型对话广场、生成历史。仅高亮导航，不触发模块跳转或生成任务；支持上一/下一步、跳过、完成。
- 画布首次进入普通编辑页时启动五步操作引导：打开添加节点、添加图片、认识节点、通过全景工具创建关联节点、查看串联结果及缩放。画布列表与只读分享页不触发。
- 当前 `createNodeNear` 只创建相邻图片节点，没有连线；画布引导使用已存在的 `createPanoramaNode` 自动连线能力，不描述尚未实现的手动拖拽连线。节点均由用户点击原有按钮创建，引导不直接写入项目数据。
- 工作流/画布分别用 `phanty-workflow-onboarding-v1`、`phanty-canvas-onboarding-v1` 记录完成或跳过。各自右下角提供重看入口；离开页面时清除高亮，与首页引导避免同时出现。
- 保持用户确认的冰蓝气泡、signpost 标题图标、标题同行右上角跳过、左下角步数。工作流为 01/06，画布为 01/05。
- 浏览器已验证工作流六步及返回/完成；空画布添加图片、生成全景并产生一条连线、缩放期间引导稳定、完成后刷新不重复、重新查看和跳过后清除遮罩。未发现浏览器错误日志。
- 本轮仅本地保存与预览，未获新增上线授权，不推送部署。

## 提交 GitHub 方法

> 固定维护区：后续更新 HANDOFF 时不要删除或改写本节和下方“部署方法”内容。每次功能更新结束都需要按这里提交并推送，以触发线上部署。

查看状态：

```bash
git status --short
```

只暂存本次需要提交的文件，不要提交 `.DS_Store`。常见静态原型变更文件如下，实际提交时按本次改动增减：

```bash
git add index.html script.js styles.css HANDOFF.md
```

提交：

```bash
git commit -m "<简短描述本次功能>"
```

推送到 GitHub：

```bash
git push origin main
```

如果需要查看最近提交：

```bash
git log -1 --oneline
```

## 部署方法

> 固定维护区：后续更新 HANDOFF 时不要删除或改写本节。当前项目依赖 GitHub push 自动触发 Vercel 部署。

本项目线上地址：

- `https://phantymovie-canvas-demo.vercel.app`

部署方式：

- Vercel 已连接 GitHub 仓库。
- 推送到 `origin/main` 后，Vercel 会自动从 GitHub `main` 分支部署。
- 通常等待 1-2 分钟后刷新线上地址即可看到更新。
- 每次完成代码更新后，都需要提交并推送到 `origin/main`；没有 push 就不会触发线上部署。

本项目当前没有 `.openai/hosting.json`，也没有本地构建步骤；页面是静态文件，部署依赖 GitHub push 触发 Vercel 自动部署。

## 注意事项

- 不要提交：
  - `.DS_Store`
  - `assets/.DS_Store`
- 项目是纯静态原型，没有后端接口。
- 视频上传使用浏览器本地 `FileReader` / `URL.createObjectURL` 模拟。
- 审核、上下架、发布状态都保存在浏览器 `localStorage` 中。
- 如果需要重置演示数据，可在浏览器中清理站点 localStorage。
